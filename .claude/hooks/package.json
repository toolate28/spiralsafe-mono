{
  "name": "hooks",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "viewer": "bun run viewer/server.ts",
    "viewer:dev": "bun --watch run viewer/server.ts",
    "test": "vitest --config viewer/vitest.config.ts",
    "test:run": "vitest run --config viewer/vitest.config.ts",
    "test:coverage": "vitest run --coverage --config viewer/vitest.config.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/coverage-v8": "^2.0.0",
    "@vue/test-utils": "^2.4.0",
    "happy-dom": "^20.3.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.62"
  }
}
